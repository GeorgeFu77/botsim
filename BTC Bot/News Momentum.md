# News Momentum #btcbot

Lean toward the side recent headlines favor (bullish tape → Up), but ONLY when the BTC model also shows edge vs the book price — a headline alone is never enough. Gates: tape ≤30min old, ≥2–4 scored headlines, |tilt| ≥ 0.2–0.6, model edge ≥ 0–0.06. Reads the [[Information Layer]] news signal.

**Hypothesis:** fresh sentiment shifts leak into 5-min pricing slower than they hit RSS. Dies if headlines are fully priced-in by parse time (likely — minutes of lag).

**Status: UNTESTED** — signal recording started 2026-07-07; no verdict until parallel data exists. Part of [[BTC Bot Strategies]].
